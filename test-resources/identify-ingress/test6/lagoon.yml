docker-compose-yaml: ../test-resources/identify-ingress/test6/docker-compose.yml

environment_variables:
  git_sha: "true"

multiproject1:
  environments:
    multiproject:
      routes:
        - node:
            - multiproject1.com
multiproject2:
  environments:
    multiproject:
      routes:
        - node:
            - multiproject2.com
