docker-compose-yaml: ../test-resources/identify-ingress/test13/docker-compose.yml

routes:
  autogenerate:
    enabled: false

environment_variables:
  git_sha: "true"

environments:
  main:
    routes:
      - node:
          - example.com
