docker-compose-yaml: ../test-resources/template-backups/test3/docker-compose.yml

backup-retention:
  production:
    hourly: 10
    daily: 10
    weekly: 10
    monthly: 10

environment_variables:
  git_sha: "true"

environments:
  main:
    routes:
      - node:
          - example.com
