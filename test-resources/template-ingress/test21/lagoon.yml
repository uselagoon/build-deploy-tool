docker-compose-yaml: ../test-resources/template-ingress/test21/docker-compose.yml

environment_variables:
  git_sha: "true"

environments:
  main:
    routes:
      - node:
          - example.com:
              alternativenames:
                - www.example.com
                - en.example.com
