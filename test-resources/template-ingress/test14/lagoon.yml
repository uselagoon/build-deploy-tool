docker-compose-yaml: ../test-resources/template-ingress/test14/docker-compose.yml

environment_variables:
  git_sha: "true"

environments:
  main:
    routes:
      - node:
          - example.com:
              ingressClass: "custom-ingress"
