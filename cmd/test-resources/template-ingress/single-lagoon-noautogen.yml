docker-compose-yaml: test-resources/template-ingress/docker-compose.yml

routes:
  autogenerate:
    enabled: false

environment_variables:
  git_sha: "true"

environments:
  main:
    routes:
      - node:
          - example.com
