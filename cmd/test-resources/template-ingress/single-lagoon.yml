docker-compose-yaml: test-resources/template-ingress/docker-compose.yml

environment_variables:
  git_sha: "true"

environments:
  fastly-annotations:
    routes:
      - node:
          - annotations.com
