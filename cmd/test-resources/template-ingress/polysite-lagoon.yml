docker-compose-yaml: test-resources/template-ingress/docker-compose.yml

environment_variables:
  git_sha: "true"

ci-multiproject1-control-k8s:
  environments:
    multiproject:
      routes:
        - node:
            - multiproject1.com
ci-multiproject2-control-k8s:
  environments:
    multiproject:
      routes:
        - node:
            - multiproject2.com
