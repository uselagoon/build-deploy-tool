docker-compose-yaml: test-resources/template-ingress/docker-compose.yml

environment_variables:
  git_sha: "true"

production_routes:
  active:
    routes:
      - node:
          - active.example.com
  standby:
    routes:
      - node:
          - standby.example.com

environments:
  main:
    routes:
      - node:
          - example.com
  main2:
    routes:
      - node:
          - example.com
