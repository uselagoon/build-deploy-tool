docker-compose-yaml: test-resources/template-autogenerated/docker-compose.yml

routes:
  autogenerate:
    prefixes:
      - www
      - de
      - fr
      - it
    enabled: true
    insecure: Redirect

environment_variables:
  git_sha: "true"

environments:
  fastly-annotations:
    routes:
      - node:
          - annotations.com
