docker-compose-yaml: test-resources/template-autogenerated/node-docker-compose.yml

routes:
  autogenerate:
    prefixes:
      - www
      - de
      - fr
      - it
    enabled: true
    insecure: Redirect

environment_variables:
  git_sha: "true"

environments:
  main:
    routes:
      - node:
          - example.com
