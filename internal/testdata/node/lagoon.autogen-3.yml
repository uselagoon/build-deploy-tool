docker-compose-yaml: ../internal/testdata/node/docker-compose.autogen-1.yml

routes:
  autogenerate:
    enabled: true
    insecure: Redirect

environment_variables:
  git_sha: "true"

environments:
  main:
    routes:
      - node:
          - example.com
