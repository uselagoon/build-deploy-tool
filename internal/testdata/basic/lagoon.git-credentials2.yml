docker-compose-yaml: internal/testdata/basic/docker-compose.yml

git-credentials:
  example_com:
    description: "passwords in variables"
    url: https://example.com
  github_com:
    description: "passwords in variables"
    # url: https://github.com # if the url would be github, it can be omit as it is the default
